---
import Layout from "../../layouts/Layout.astro";
---
<Layout>

</Layout>

<script type="module" defer>
  //! Variables 
  // https://javascript.info/variables
  let message1 = 'Hello!'; // define the variable and assign the value

  let message2; // define the variable
  message2 = 'Hello'; // assign the value

  // Old Let(Var): https://javascript.info/var
  // USE With CATIOUS

  //! Constants 
  // https://javascript.info/
  const COLOR_RED = "#F00";// define the variable and assign the value

  //! If 
  if(message1<1){}
  else if(message1<2){}
  else{}

  //! ? 
  let result1 = (message1==2) ? console.log("Value1") : console.log("Value2");

  //! While 
  while (message1<1) {
    console.log("While Ran 1ce")
  }

  //! Do While
  do{
    console.log("Do While Ran 1ce")
  }while(message1<1);
  
  //! For
  for (let i = 0; i < 1; i++) {
    console.log("For Ran 1ce")
  }

  //! Switch 
  switch (message1) 
  {
  case 1:
    console.log("Switch Ran 1ce")
    break;
  case 2: // (*) grouped two cases
  case 3:
    console.log("Switch Ran 1ce")
    break;
  default:
    console.log("Switch Ran 1ce")
  }

  //! Function 
  function showMessage1() {
    console.log("Function Ran 1ce")
  }
  showMessage1(); // Calling the function

  // Function With parameters
  function showMessage2(message) {
    console.log({message})
  }
  showMessage22("Function Ran 1ce")

  // Default values
  function showMessage2(message= "No Text To Show") {
    console.log({message})
  }
  showMessage22("Function Ran 1ce")

  // Function Expressions (Any Use Real World Case?)
  // https://javascript.info/function-expressions
  let sum1 = (a, b) => a + b;

  //! Arrow Functions
  
  //! Object
    let user1 = new Object(); // "object constructor" syntax
    let user2 = {};  // "object literal" syntax
    let user3 = {     // an object
      key: "value",  // by key "key" store value "value"
    };
    for (key in user3) {
      // executes the body for each key among object properties
      // keys
      alert( key );  // name, age, isAdmin
      // values for the keys
      alert( user[key] ); // John, 30, true
    }

  //!
  //!
  //!
  //!
  //!
  //!
  //!
  //!
  //!
  
</script>